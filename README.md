# Fund NAV Auto Update

This repository automatically updates daily NAV data for configured funds.

The update process is intended to run via GitHub Actions on a schedule, and writes the latest NAV values to CSV files in the `data/` folder.
